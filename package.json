{
  "name": "mi-carpeta-segura",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "local": "node scripts/local.mjs",
    "local:parar": "docker compose stop",
    "local:limpiar": "docker compose down -v --remove-orphans",
    "local:e2e": "npm --prefix e2e ci && npx --prefix e2e playwright install chromium && npm --prefix e2e test"
  }
}
